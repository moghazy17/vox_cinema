.env
.env.*
!.env.example

# Local Claude Code settings — machine-specific, and permission entries can
# embed literal secrets. Never commit.
.claude/settings.local.json

# Cloudflare Worker scheduler — local secrets and build/dev artefacts.
scheduler/.dev.vars
scheduler/node_modules/
scheduler/.wrangler/

__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
